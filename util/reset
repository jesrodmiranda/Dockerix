#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

sudo rm -f /usr/local/bin/docker*
pkill VirtualBox
pkill VBox
rm -rf ~/Library/Application\ Support/Dockerix/
rm -rf ~/.docker
rm -rf ~/Library/VirtualBox/
rm -rf ~/Dockerix
$DIR/VirtualBox_Uninstall.tool
